source 'https://rubygems.org'

gem 'rails', '4.2.1'
gem 'bootstrap-sass', '~> 3.3.0'
gem 'pg'
gem 'sass-rails', '~> 5.0'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.1.0'
gem 'jquery-rails'
gem 'jbuilder', '~> 2.0'

# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

#bower
gem 'bower'

#to get paperchip for image uploads
gem 'paperclip'

# For Bower on Heroku
gem 'rails_12factor', group: :production

# Debugging tools
gem 'better_errors'
gem 'binding_of_caller'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

gem 'devise'

group :development, :test do
  # Debugging tools
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'pry-rails'
end

